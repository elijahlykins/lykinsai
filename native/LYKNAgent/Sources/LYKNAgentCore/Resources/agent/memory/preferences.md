# Preferences

Stable user preferences that shape how tasks are performed (preferred
retailers, writing tone, default airports, dietary constraints, etc.).

Nothing stored yet. Entries are added at runtime and kept in the user-data
memory store, never in this template.
