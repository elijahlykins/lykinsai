/**
 * Soft-unplug Model Builder custom models / sub-agents from chat + voice.
 * Tools and UI stay in the repo; product paths skip them while this is false.
 */
export const CUSTOM_MODELS_ENABLED = false;
