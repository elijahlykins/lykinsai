/** Shared with the server routing policy. Keep in sync with lib/models/routingPolicy.js. */
export const ROUTE_CATEGORIES = Object.freeze([
  'default',
  'quick',
  'reasoning',
  'coding',
  'vision',
  'research',
  'agents',
]);
