# Website Memory

One file per website host (e.g. `amazon.md`, `gmail.md`) containing useful
semantic knowledge learned about that site: where features live, quirks,
effective strategies.

Store semantic knowledge ("search filters are in the left sidebar"), not
fragile CSS selectors. Runtime-learned notes are kept in the user-data memory
store; files here act as optional seeds.
